subprojects {
    apply plugin: 'java-library'
    java.toolchain.languageVersion = JavaLanguageVersion.of(17)

    repositories {
        mavenCentral()
        maven { url = 'https://maven.moddingx.org' }
        maven { url = 'https://maven.minecraftforge.net' } // For SrgUtils
    }
}
